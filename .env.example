# QuickServe IT - Environment Configuration
# Copy this file to .env.local and fill in your actual values

# API Configuration
VITE_API_URL=http://localhost:3000/api
VITE_APP_NAME=QuickServe IT
VITE_APP_VERSION=1.0.0

# Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG_MODE=false

# Database Configuration (if using backend)
# DATABASE_URL=postgresql://user:password@localhost:5432/quickserveit

# Auth Configuration (if using authentication)
# AUTH_SECRET=your-secret-key-here
# AUTH_PROVIDER=local

# File Upload Configuration
# MAX_FILE_SIZE=5242880
# ALLOWED_FILE_TYPES=pdf,doc,docx,xls,xlsx,csv

# EmailJS Configuration (Client-side email service - NO BACKEND REQUIRED)
# Get these from https://www.emailjs.com/
# 1. Create a free account at EmailJS
# 2. Add an email service (Gmail, Outlook, etc.)
# 3. Create an email template with variables: from_name, from_email, from_mobile, service_type, message
# 4. Get your Public Key from Account settings
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# Email Configuration (if sending emails via backend - NOT USED, using EmailJS instead)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@quickserveit.online

# Deployment
VERCEL_ENV=development
